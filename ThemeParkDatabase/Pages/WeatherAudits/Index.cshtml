@page
@model ThemeParkDatabase.Pages.WeatherAudits.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
                <th>
                    @Html.DisplayNameFor(model => model.WeatherAudit[0].WeatherAuditId)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.WeatherAudit[0].Rainout)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.WeatherAudit[0].Temperature)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.WeatherAudit[0].InchesPrecipitation)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.WeatherAudit[0].Date)
                </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.WeatherAudit) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.WeatherAuditId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Rainout)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Temperature)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.InchesPrecipitation)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Date)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.Id">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
